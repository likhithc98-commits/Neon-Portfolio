{
  "name": "challampalam-likhith-portfolio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Development mode'",
"build": "sh -c 'cd Neon-Portfolio && npm run build && mkdir -p ../dist && cp -r dist/public/* ../dist/'"      },
  "dependencies": {},
  "devDependencies": {}
}
